# CLAUDE.md

## Agent skills

### Issue tracker

Issues 跟踪在 GitHub Issues（使用 `gh` CLI）。See `docs/agents/issue-tracker.md`.

### Triage labels

五个分流标签：`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`。See `docs/agents/triage-labels.md`.

### Domain docs

Single-context：repo 根目录 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`.
